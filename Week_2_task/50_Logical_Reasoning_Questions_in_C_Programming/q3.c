#include<stdio.h>
int main()
{
	int x = 10;
        printf("%d", x == 10 ? 100 : 200);
       	// it is ternary operator if it is true it will print 100 
	// unless 200 will be print 
        return 0;

}
